import { SwitcherTheme } from "@/ui/components/atomic/index";

function Header() {
    return (
        <header id="header">
            <SwitcherTheme />
        </header>
    );
}

export default Header;
