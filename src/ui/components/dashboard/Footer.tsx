import { Typography } from "@mui/material";

function Footer(): React.ReactElement {

    return (
        <footer id="footer">
            <Typography>
                Hackaton
            </Typography>
        </footer>
    );
}

export default Footer;