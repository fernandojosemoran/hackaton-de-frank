import { SwitcherTheme } from "@/ui/components/atomic/index";

function Header() {
    return (
        <header id="header">
            <h1>Inventary management</h1>
            <SwitcherTheme />
        </header>
    );
}

export default Header;
